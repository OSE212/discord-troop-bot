"""Discord-facing layer: commands, interactions (modals/views), and
permission checks. Business logic lives in bot.services and
bot.optimizer -- this package should stay a thin adapter over those.
"""
