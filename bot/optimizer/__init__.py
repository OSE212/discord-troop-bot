"""Deterministic, rule-based formation optimizer.

Nothing in this package uses an LLM to decide troop assignments (see
spec section 1 / rule 10). All scoring comes from configurable weights
in bot.rules.configuration.
"""
